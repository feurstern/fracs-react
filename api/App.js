const express = require('express');
const app = express();
const port = 3001;

app.get('/', (req,res)=>{
    res.send("Connected succsefully");
});

app.listen(port,()=>{
    console.log(`The port used is ${port}`);
})

}`);
})

}`);
})

